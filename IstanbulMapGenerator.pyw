#Add the path to the clipboard_utils module in the line below if in different folder
#import sys
#sys.path.append(r'')

import IstanbulMapGenerator

tk = IstanbulMapGenerator.create_GUI()
tk.mainloop()
